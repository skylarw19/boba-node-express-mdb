
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <link href="https://fonts.googleapis.com/css?family=Indie+Flower&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="/stylesheets/main.css" />
  <title>Boba Drinker</title>
</head>
<body>
        <% if (user) { %>
            <%- include('../partials/nav') %>
            <a href="/users/<%=user._id%>/visitedStores">My Visited Stores</a>
            <a href="/logout">Log Out</a>
        <% } else { %>
            <a href="/auth/google">Login with Google</a>
        <% } %>
    </nav>
  <div>
      <!-- only allows post if user is in db-->
    <% if (user) { %> 
        <!-- if loggedin user matches the user it's on -->
        <% if (user._id.equals(user && user._id)) { %> 
            <div>
                <p>Welcome, <%=user.name%>!</p>
                <p>Check out the list of boba stores to add one you've already visited!</p>
            </div>
        <% }} %>
  </div>
</body>
</html>


